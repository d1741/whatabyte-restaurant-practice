//- To optimize template creation, we need a layout to encapsulate the top-level HTML structure of our page (aka "template inheritance")
block variables
doctype html
html
    head
        meta(charset="utf-8")
        link(rel="shortcut icon", href="/favicon.ico")
        meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")
        meta(name="theme-color", content="#000000")
        //- title variable will be passed from the server to the template
        title #{title} | WHATABYTE
    body
    div#root
      block layout-content